import 'package:flutter/material.dart';

class CaptainScreen extends StatelessWidget {
  const CaptainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Captain Screen'));
  }
}
